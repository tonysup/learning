虚拟内存

操作系统提供了在单台机器上抽象的方法

通过虚拟化强制模块化，分三个层面：
(1)虚拟内存(内存)
(2)多线程(处理器)
(3)有界缓冲区的发送和接收(通信链)

该小节主要讲虚拟内存：
操作系统的MMU 用来映射虚拟内存和真实的内存地址，并且使用page tables


使用物理地址和虚拟地址的一一映射，占用内存过大----->使用页+偏移量的方式，减少映射占用的内存，并且偏移量有部分字段表明该地址的操作属性，是否在内存中等。
-------->进一步优化，提出page to page的，页层级关系，减少页映射的大小。
