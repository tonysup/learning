性能

1.度量
(1)容量 利用率 开销以及有用的工作
(2)延迟
(3)吞吐率

2.减少延迟的方法
(1)利用工作负载的特性减少延迟,最常用的就是缓存
(2)利用并发性减少延迟

3.消除瓶颈的方法
(1)批处理
(2)延迟处理
(3)推测执行

多层存储
从寄存器到网络远端计算机存储 一个金字塔型关系

缓存局部性以及管理策略(页面替换算法) ---FIFO LRU MRU 等

资源调度策略(算法) ---先到先服务，短作业优先，轮转策略，优先级调度
